[pytest]
# pytest-asyncio handles all async tests; disable the other async plugins
# (tornasync/trio/twisted) so they don't intercept coroutine tests and
# leave them un-awaited
addopts = -p no:tornasync -p no:trio -p no:twisted
asyncio_mode = strict
