[project]
name = "blightsanest-migrations"
version = "0.1.0"
description = "BlightSanest database schema: Alembic migrations for the SQLAlchemy models in ../models"
requires-python = ">=3.12"
dependencies = [
    "alembic>=1.20.0",
    "psycopg2-binary>=2.9.13",
    "sqlalchemy==2.0.51",
]
